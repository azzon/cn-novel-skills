# 红队审计: 流水线工具(tools/*.py + pre-commit hook)

- 日期: 2026-09-15 | 审计对象: tools/ 全部23个*.py + tools/pre-commit-hook.sh
- 方法: 逐文件读主逻辑(~5300行) + 在临时书根(ROOT/_rt_probe、/tmp,测后已清理)构造对抗样本实跑
- 判定分级: **P0=可绕过(硬门失效)** / P1=误报漏报 / P2=健壮性
- ⚠ 审计期间工作区被并发修改(tools/{skill_protocol,pipeline,genre_contract,system_readiness}.py 于19:51-19:54变更,含"红队20260915"字样的修补)。本报告行号与结论基于**19:54后的现行版本**;其中一项修补本身有缺陷(见P0-5)。

## 结论速览

- **空放过(vacuous pass)总计 27 处**: 死代码/永不触发的检查 9 处 + 存在性/token级检查(技术通过、实质未查)18 处。另有 check.py 约30项 WARN 无任何机器强制(仅文案提醒)。
- 最危险三连: ①先commit再done → 全部流程门降WARN(live实证 exit 0);②G6时序门死代码(时序回退章全绿);③八账/技能记录/冷读全部token级盖章,垃圾文本即可通过。
- self_test.py 现状14组全绿——但它不覆盖上述任何一项(覆盖缺口本身就是发现)。

---

## P0 可绕过(硬门失效)

### P0-1 done 的 post 模式把全部流程门降为 WARN("先提交后验收"全放行)
- **位置**: tools/pipeline.py:628-633(committed → `revise=True; post=True`),795(八账)、736-777(技能执行记录/章摘要/人物圣经/生成记录/卷末义务)全部 `(warns if (revise or post) else problems)`
- **攻击样本**: 章节过了 check.py 内容门后**先 git commit 再跑 done** → 缺场景卡、无技能执行记录、无章摘要、无生成记录、八账零盖章、无卷末审查——全部只剩 WARN,**exit 0 "验收通过"**。AGENTS.md 宣称"未全勾=problems拦截"即被绕过。live 实证(伪造 git 仓库+1993真实第002章):
  ```
  ✅ 第001章验收通过: progress+scores已重算
    [WARN] 缺场景卡 … [WARN] 无技能执行记录 … [WARN] 八账未盖章: ['伏笔','梗',…]
  DONE_EXIT=0
  ```
- **复现**: 见本审计 live 过程——`git init 假根 && cp 过check的章 第001章.md && echo '总分:9' > audit/冷读-第001章.md && git commit 后 python3 tools/pipeline.py done 1 --book bk1`
- **修复**: post 模式只允许刷新 scores.committed,**不得**降级流程门;可从 git log 验证的项(生成记录时间戳、账本是否在 commit 前含本章)在 post 模式仍走 problems。

### P0-2 G6 时序门: FAIL 分支是不可达死代码
- **位置**: tools/gate_chapter.py:338 `if mm < n: tl_max = max(tl_max, mm)` → :341 `if tl_max and mode=="new" and n < tl_max`——tl_max 按构造严格小于 n,`n < tl_max` **恒假**
- **攻击样本**: 时间线账已记到第10章,新写第005章(时序回退,无"插叙:"标记)→ gate **PASS exit 0**(live 实证,仅 G1 字数 WARN)。文档与注释宣称的"硬化(audits/13攻击7)"实际从未生效;当年修"账本超前致误判"时把整个门修死了。
- **复现**: 书根内4个存量章 + `ledgers/时间线.md` 写 `- 第010章|…` + 新章 `第005章.md`(≥1500字) → `python3 tools/gate_chapter.py new <path>` → PASS
- **修复**: 检测目标改为"存在 mm > n 的账面记录且无插叙标记":`tl_ahead = max((mm for mm>0 if mm>n), default=0); if tl_ahead and mode=="new": FAIL`

### P0-3 check.py #47 金额算术器: 定义后零调用,整段死代码
- **位置**: tools/check.py:876(注释宣称"同章同名科目出现两个不同值=FAIL")、878(`_fig` 正则)、879-907(`_zh2int` 函数)——grep 实证 `_fig.` 使用 **0 次**、`_zh2int(` 出现 **1 次**(即定义本身)
- **攻击样本**: 正文"这个月流水三千块…第二天再看账,流水五千块"——同科目两值钱面矛盾,check.py 零报警(实跑确认)。
- **复现**: `python3 -c "…写入两段流水三千/流水五千正文…" && python3 tools/check.py --modern --scene /tmp/x.md | grep 流水`(无输出)
- **修复**: 补对账循环(`for m in _fig.finditer(body)` 按科目分组比 `_zh2int` 值),或删除该节注释防止"以为有这道门"。同型死代码: check.py:612-613(`if…: pass`)、640-641(`if _peak_explain>=2: pass`,真检查在837)、1072-1076(重复的 `if len(args)<2`);evals.py:42(`"--scene" if False else fp` 死表达式)、194(`new_s is not None and new_s is not None` 重复条件)。

### P0-4 ledger_stamped: token 级盖章,垃圾一行即通过
- **位置**: tools/pipeline.py:149-166(现行版已要求"- "行+过滤"待记|TODO|占位|待补",但**仍只查行内含"第N章"token**)
- **攻击样本**(live 实证): 8个账本各写一行 `- 第005章 空话一行测试用` → `ledger_stamped(5)` 返回全部8账,done 的"八账未盖章"拦截为空。账本内容与正文完全无关。
- **复现**: `for led in 伏笔 梗 钩分布 类型轮换 人物状态 线弦 时间线 数字账; do printf -- "- 第005章 空话\n" > 书根/ledgers/$led.md; done` → `python3 -c "import sys;sys.path.insert(0,'tools');import pipeline as P;…P.ledger_stamped(5)"`
- **修复**: 盖章行须结构化(如`科目|限定|值|章`),且抽查值在正文可对账(number_audit 已有该能力,串联进 done);钩分布/时间线章行须与 check.py METRICS 的 ending_shape/日期词交叉验证。

### P0-5 skill_protocol audit 证据链: "0"字自证 + `--evidence` 是死旗标(新修补自身有缺陷)
- **位置**: tools/skill_protocol.py:98-99(`ref.lower().startswith(("exit","0","1"))` → 直接 pass——**任何以0/1开头的引用串都当exit码放行**,且不区分 script/skill 步骤);main():255(`cmd_audit(n, book)` 未传 evidence;`--evidence` 不在 main() 解析)→ pipeline.py:735-741(n≥33 新硬门调用`--evidence`)实际**传不进去**,no_ev(打勾无产物引用)永不拦截
- **攻击样本**(live 实证): 技能执行记录全部写 `- [x] xxx → 产物: 0` → `audit 5 --book … --evidence` → **EXIT=0,零WARN**。产物引用还可指向章节自身(`→ 产物: text/卷1/第005章.md`,存在且含章token,通过)。
- **复现**: `printf -- '- [x] a 技能: x → 产物: 0\n' > _rt/ledgers/技能执行记录.md && python3 tools/skill_protocol.py audit 5 --book _rt --evidence; echo $?`(0)
- **修复**: main() 解析 `--evidence`;exit码引用须严格形如 `exit=0`;产物引用按步骤类型白名单(script步骤=exit码,agent步骤=.md路径且不得为正文自身)。

### P0-6 冷读报告: 纯存在性检查,总分为自填,无任何内容校验
- **位置**: tools/pipeline.py:101-106(cold_read_for 只 glob 文件名)、672-675(done 第5步只查文件存在);pre-commit-hook.sh:138(只 `grep -s "总分"`——两个字即可);exemplar_flywheel.py:42-49(从自填冷读收割"最强段落"进范例库,污染后续 bundle 注入)
- **攻击样本**(live 实证): `printf '# 冷读\n总分:9\n垃圾内容\n' > audit/冷读-第001章.md` → 硬门冷读章全过;flywheel harvest 把"最强段落摘录: 天气热得不行他擦了把汗这是随便编的垃圾段落测试用"原句收进 风格包范例段库.md,此后每章 bundle 注入它当"本书最佳文字"。
- **复现**: 如上两条命令 + `python3 tools/exemplar_flywheel.py harvest 书根 && head 书根/风格包范例段库.md`
- **修复**: 冷读骨架(gen coldread)生成结构化分项(六项量规各带分),audit 校验 分项和=总分 且 任一分项低于下限即打回;flywheel 收割前校验该冷读总分≥阈值且摘录段原文存在于该章正文。

### P0-7 check.py 对话占比: 含引号段落整段计入,叙述挂2字引号即98%
- **位置**: tools/check.py:309-316(`if "\u201c" in pp` 整段字符计入 dialog_chars;#16 35%硬线基于此)
- **攻击样本**(live 实证,真实章节): 1993书第002章基线 `dia_char_pct=45.4` → 每个无引号叙述段尾部追加 `他应了一声。“嗯。”` → **`dia_char_pct=98.1`,状态仍 WARN 通过**。#48语气词/#49碎片率同样只看引号内,"嗯/行吧/得嘞"各一枚即可充数。
- **复现**: `python3 tools/check.py --modern --metrics --scene <改造章> | grep -o '"dia_char_pct": [0-9.]*'`
- **修复**: 对话字符口径改为 引号内字符 + 引导句窗口(±20字);或对"引号内<10字且段落>80字为叙述"的段打折计。

### P0-8 voice_check: 无 tag 对白零归属 → 零检查
- **位置**: tools/voice_check.py:57-88(attribute_dialogure 依赖"人名+说/道/…"tag 建立归属;从未 tag → alternation 空 → cur=None → 引号不计入任何角色)
- **攻击样本**(live 实证): 声口卡王大龙禁词"格局/逻辑",正文通篇无 tag 对白连说三遍"格局要打开,逻辑要闭环" → **"声口卡全项通过" PASS exit 0**。
- **复现**: `python3 tools/voice_check.py /tmp/无tag对白.md --card 测试声口卡.md; echo $?`(0)
- **修复**: 统计"归属覆盖率"(有归属引号段/总引号段),<50% 时 WARN;hard 归属为 0 时输出"本章声口门无效(无tag)"而非 PASS。

### P0-9 中文数字章名 = 幽灵章,穿透全部编号门
- **位置**: tools/gate_chapter.py:67-69(parse_num 只认 `第(\d+)章` → None)→ G2/G2b/G4/G6 全部以 `n is not None` 为前提**整体跳过**;recompute/chapter_map 同样剔除;pre-commit-hook.sh:24(NEW_CH awk 要求 `第[0-9]+章`)→ 不入章节流,只触发 TEXT_ODD **WARN**(hook:37,96-100)
- **攻击样本**(live 实证): 存量第001-005章之外新增 `第五章.md`(1800字) → `gate_chapter.py new` → **PASS exit 0**;该章不占章号、可绕过重复/跳章/卷归属/时序全部检查,入库后成为账本永远对不上的幽灵文件。
- **复现**: `python3 -c "print('第五章 幽灵章\n\n'+'丙'*1800)" > 书根/text/卷1/第五章.md && python3 tools/gate_chapter.py new 书根/text/卷1/第五章.md`
- **修复**: gate/hook 对 `第*章.md` 命名中非阿拉伯数字者直接 FAIL"章名不规范";parse_num 增加中文数字解析作兜底。

### P0-10 anticipation_audit: 无条件 print PASS、恒 return 0
- **位置**: tools/anticipation_audit.py:92-93(所有输出都是 WARN;exit code 恒 0)
- **攻击样本**(live 实证): 钩账 5/5 章全部"叙述收"(钩力全灭) → 输出 `期待链审计: PASS(2提醒)`、exit 0。system_readiness 把它当健康块(永远 PASS)。
- **复现**: `printf -- '- 第001章 [叙述收] 甲\n…5行…\n' > 书根/ledgers/钩分布.md && python3 tools/anticipation_audit.py 书根; echo $?`
- **修复**: 连击≥3 或 叙述收占比>60% 时 exit 1。

### P0-11 genre_contract: premise 写"封存"四个关键词之一 → 全部 FAIL 降 WARN
- **位置**: tools/genre_contract.py:36-37(`parked = re.search(r"待重启|清稿|搁置|封存", premise)` → bucket=warns)
- **攻击样本**(live 实证): 前提写"本书封存待重启。他重生回1993年。" → 缺流派声明+缺矿产三件套全部降 WARN → `流派契约: PASS` exit 0。
- **复现**: `python3 tools/genre_contract.py 书根`(premise 含"封存"且无流派)
- **修复**: parked 状态须落在独立状态文件(如 `书根/.parked`)+仪表盘单列"PARKED",不允许 premise 正文关键词自我豁免。

### P0-12 foreshadow_audit: "顺延"二字吞掉全部告警;除重复ID外恒 exit 0
- **位置**: tools/foreshadow_audit.py:79-80(状态含"顺延|逾期|待收"→ continue,**连已跳票的排期检测一并跳过**);99(return 1 仅当 ID 重复)
- **攻击样本**(live 实证): 两个登记"收:ch3/ch2"已跳票的伏笔标 `状态:顺延` → `伏笔账全项健康` PASS(0提醒)。
- **复现**: `printf -- '- [F-1] x | 埋:第1章 | 收:ch3 | 状态:顺延\n- [F-2] y | 埋:第1章 | 收:ch2 | 状态:顺延\n' > 书根/ledgers/伏笔.md && python3 tools/foreshadow_audit.py 书根`
- **修复**: 顺延必须带新排期(`顺延:chN`),`cur > 新排期`照报;无新排期的裸"顺延"= WARN 不豁免。

---

## P1 误报/漏报

### P1-1 G1 字数门: 判决阈值与文案互相打架
- gate_chapter.py:244 判 `cn < 1500`,:260 文案"`(<2500硬线)`";:263 判 `<2000`,:264 文案"`(<3000,目标3500-5000)`"。live 输出"新章仅1405字(<2500硬线)"——实际硬线是1500。读者/执行者按文案理解与机器行为相差1000字。修复: 统一为同一常量。

### P1-2 check.py 多书隔离残缺(主书资产作用于所有书)
- check.py:99-115 `_load_voice` 恒读 `ROOT/story/20-人物/声纹表.md`(实证加载主书 VOICE_NAMES=['马小丁','苏棠',…] 用于任何书根章节的#20/#23);:758-762 `#62` 恒读 `ROOT/story/60-圣经/声口卡.md` 并**硬编码主书十个人名**;:939 `.era2005` 只认主书 text/。pipeline.py:177/856 `--modern` 只认 `ROOT/text/.modern`——hook:108 是对的(`$BDIR/text/.modern`),pipeline 是错的(书根古代书会被主书标记污染;live: 伪造根无 .modern 导致古代章误 FAIL 时代错位词)。修复: 三处均按 `book_root(fp)` 解析。

### P1-3 gate_chapter.volume_decl 恒读主书卷册表
- gate_chapter.py:92-101(`ROOT / "story" / "30-情节" / "卷册表.md"`),expected_volume:103-118 使用之 → 书根的 G4 卷归属实际按**主书**卷册表判定。修复: 按 book 参数取 `book/story/30-情节/卷册表.md`。

### P1-4 voice_check 默认卡/ card_check 默认寻卡均为跨书
- voice_check.py:26(DEFAULT_CARD=主书声口卡;CLI 不带 --card 即用主书禁词查他书正文);card_check.py:95(无 --book 时 `[ROOT] + 全部一级子目录` 首命中即返——双书同章号错卡,注释自认 S3 但缺省仍歧义)。修复: 从章节路径推导书根。

### P1-5 card_check 数字对账上下文无关
- card_check.py:56-90(extract_vals 收集**全正文**数值集合,:171 集合差判定)。live: 卡载"角膜浑浊约12小时/编号3"由正文"**12**路公交""**3**号楼"兑现通过。修复: 条目语境词(如"角膜|浑浊")须与数值同句/邻窗共现。

### P1-6 pre-commit hook: tools-only 提交跳过 self_test 与 skills_check
- pre-commit-hook.sh:40(早退条件不含 tools)、228-247(F 块整体包在 `if [ -n "$SKILLS_STAGED" ]` 内)。只 stage `tools/*.py` 时 → "(无staged脚手架产物)"后**early-exit PASS**,自测与技能体检全不跑。当前工作区4个工具改动若如此提交即无任何机器验证。修复: `if [ -n "$SKILLS_STAGED" ] || git diff --cached --name-only | grep -qE '^tools/'`。

### P1-7 structure_check 小样本降级写反
- structure_check.py:83 `eff = max(pct, pct*n/10)`: n<10 时 eff=pct(**降级失效**),n>10 时 eff=pct*n/10(**占比放大**): n=20、pct=35% → eff=70% 误 WARN;而 n=8、pct=65% 本应降级却原样触发。修复: `eff = min(pct, pct*n/10)`。

### P1-8 system_readiness: 正文全 FAIL 只算"带病可开"; card_check 结果被丢弃
- system_readiness.py:58-64(check 全量失败 → 块状态 WARN,不进 hard_fail——全书每章 FAIL 也判 🟡);67-68(evals 回归最多 WARN);89(`r = run(["tools/card_check.py","001",…])` 结果 **r 从未使用**,card_note 只读对账清单文件)。修复: FAIL 率>30% 升 FAIL;card_check 输出接入。

### P1-9 evals: 恒 --modern、冒烟写真实主书、书根基线被主书 bundle 污染、崩溃兜底成"0 FAIL"
- evals.py:42(恒传 `--modern`,古代书基线时代词检查被关);55-62(check.py 崩溃 → 无"汇总"行 → fails=0,仅 cjk=0 间接兜底);128-136(冒烟章写入**真实** `ROOT/text/卷1/第999章.md`,书根基线也写主书);139(`bundle 2` 硬编码主书章号——书根 collect 的 bundle_cropped 数据来自主书)。修复: 冒烟走 tmp;书根模式传书根下一章号;崩溃时显式记 FAIL。

### P1-10 quality_score 冲突维度用通用连接词(与 check.py 自我修订矛盾)
- quality_score.py:51-55("问题|麻烦|但是|可是|然而|急|查"计为冲突——check.py:615-621 已把同类词表斥为"any()恒真=死检查"并删除,**两工具口径自相矛盾**);:59 感官词表含"热|冷|疼|苦|干|硬"等常用字。均分被近恒真维度灌水 → evals"单章骤降>8分"回归检测灵敏度被稀释。另: :94-95 两条 `$` 锚正则缺 re.M,只查全文末尾。修复: 冲突维度换强词表(与 check.py #53 同源)。

### P1-11 ledger_compact: 一次性压缩(幂等头导致永不再压)
- ledger_compact.py:25-26(fold)、46(keep_tail): 归档头一旦存在即 `return 0`。live: 160 条"- 第"行、第三次运行**归档 0 条**。"百万字长跑韧性"在第一次压缩后即失效,账本恢复无限膨胀。修复: 归档区与活跃区分开统计,活跃区超 keep 即再折叠。

### P1-12 goldmine/genre 自声明豁免(fail-open)
- goldmine_audit.py:46-52(premise 无"重生|穿越|先知…"关键词 → 整审计跳过 return 0——重生书只要不在 00-前提.md 写这些词即免检);genre_contract.py:31-33(无 00-前提.md → WARN+return 0)。修复: 书根存在 text/ 正文但无前提文件时按 FAIL 处理。

### P1-13 pipeline bundle 范例段收割的三元优先级缺陷
- pipeline.py:415 `elif _l.startswith("- 第") and _cur in (_want,"动作") if _want != "生活" else _cur == "生活" and len(_segs) < 2:` —— want=生活 分支不检查 `- 第` 前缀,段落下任何行都可能入 _segs。修复: 加括号明确分组。

### P1-14 recalc_progress 书根失效 + done 卷末审查目录错位
- pipeline.py:194-196(恒写 `ROOT/.progress.json`;`--book X` 时书根 progress 永不更新);770-773(`(BOOK/"audit").glob("*连读审查*")`——主书 audit 实际在 `story/audit`,ROOT/audit 不存在 → 主书卷末章对未提交新章误发 problems"缺卷级连读审查"即误拦)。修复: recompute 支持 --book;主书路径取 `ROOT/story/audit`。

---

## P2 健壮性

### P2-1 吞错点清单(fail-open)
- gate_chapter.py:212-213(`except OSError: pass`——G8 语料静默缺失=跨章贴入门失效)、:313-315(`except Exception: continue`——G5 跳过存量章)
- pipeline.py:552-555、721-723、886-889(`except Exception: pass` 吞掉一切;其中 **:894 `bundle_log(n, total)` 在 cmd_scores 作用域引用未定义变量 → 每次跑 scores 必抛 NameError 被吞**,live 实证 exit 0 且无落盘)
- 修复: 吞错至少打印一行 stderr;删掉 cmd_scores 尾部的死调用。

### P2-2 缺文件/异常输入直接崩栈
- voice_check.py:107(章节文件不存在 → read_text 裸抛 Traceback;对比声口卡缺失有友好 return 2);gate_chapter.py:273(G3 标题门对每个存量章 read_text 无 try,单个坏文件崩整个 gate)。中文文件名 OK(全链路 pathlib+quotepath=false 已处理);空文件被 check.py:144"空文件"拦。

### P2-3 hook 内 fix_quotes 链 fail-open
- pre-commit-hook.sh:147(`python3 fix_quotes … && git add` —— fix_quotes 崩溃时无 FAIL 置位,静默跳过)。

### P2-4 workflow.py 幽灵功能与 fail-open 默认
- workflow.py:9(docstring 宣称 `check-phase` 命令,main():157-176 无此分支);check_gate_condition:103-107(数字检查恒 True、默认 `return True`)。load_yaml_simple 丢弃 name/type/skill 字段(cmd_next 显示空洞)。

### P2-5 账本命名约定脆弱(补零不一致)
- hook:105/116-119(CH_NUM 取自文件名=零填充"001",grep "第001章"——账本若写"第1章"则误拦);pipeline ledger_stamped 双格式兼容。两处口径不一,混合写法必炸一边。

### P2-6 仓库外路径回退主书
- gate_chapter.py:27-37(book_root 对 /tmp 等外部路径 return ROOT → 用**主书**存量章当语料跑 G5/G8 查重,live 首跑实证)。修复: 找不到书根时明确报错退出。

### P2-7 杂项
- card_check.py:141(数字表只匹配单行、:145 只按"/"切分,多行/顿号表漏项);:156(issues/warns 重复初始化);genre_contract.py:16-17(KNOWN 12 个 vs 文档宣称14流派);check.py:1092(threads 模式无 threads.txt 时打印后 return 0——"无可扫描"被当通过)。

---

## 空放过(vacuous pass)汇总

| 类别 | 数量 | 条目 |
|---|---|---|
| 死代码/永不触发 | 9 | G6时序门、check#47金额算术器、check:612、check:640(冗余)、check:1072、evals:42、evals:194、skill_protocol --evidence死旗标、workflow check-phase幽灵 |
| 存在性/token级(技术通过实质未查) | 18 | 冷读存在性(done+hook"总分"grep)、冷读总分自填、flywheel垃圾收割、八账token盖章、章摘要/生成记录/人物圣经演进层/时刻卡4项token、技能记录打勾自证+"产物:0"、card_check数字上下文无关、对话占比整段计数、voice无tag零检查、钩子信号词表过宽(check#31"一声/来了/开门"即算钩)、quality_score冲突+感官2维近恒真、goldmine+genre自声明跳过2处、anticipation恒PASS、foreshadow顺延吞 |
| WARN无强制 | ~30+ | check.py 全部 WARN 项(仅文案提醒"必须处置",无 waivers 核对机器)、done post模式8项流程门降WARN、hook evals回归WARN、TEXT_ODD WARN |

## 修复优先级建议

1. **今日**: P0-1(post模式)、P0-2(G6)、P0-5(--evidence接线+"0"拦截)——三处都是几行改动。
2. **本周**: P0-4/6/7/8/9(盖章结构化、冷读分项校验、对话口径、归属覆盖率、章名规范)、P1-6(hook tools门)。
3. **排期**: 多书硬编码清理(P1-2/3/4)、self_test 补齐上述所有攻击样本为靶测用例(当前14组全绿给了虚假安全感)。
